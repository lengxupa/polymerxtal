name: test
channels:
dependencies:
    # Base depends
  - python
  - pip

  - numpy
  - matplotlib
  - openbabel
  - scipy
  # - ovito # visualization script and not used for testing
  
    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

