name: test
channels:
dependencies:
    # Base depends
  - python
  - pip

  - numpy
  - matplotlib
  - ovito
  
    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

